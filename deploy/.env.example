# host port for the web entry (nginx). Use 80 for a real deployment.
WEB_PORT=80

# database credentials (change before production)
DB_USER=comic
DB_PASSWORD=change-me-in-prod
DB_NAME=comic

ALLCPP_BASE_URL=https://www.allcpp.cn
